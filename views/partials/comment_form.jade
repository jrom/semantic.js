if user
  form.comment-form(action= '/comment', method= 'post')
    textarea.js-autoresize(name='body', id='input-body', placeholder='Digues la teva! Escriu aquí el teu comentari...')
    input(type='hidden', name='item_id', value=item._id)
    button.button.white.send(type='submit') Enviar comenatri
else
  p
    | Identifica't per escriure un comentari
    a.login.button(href='/login/twitter') Log in via Twitter
